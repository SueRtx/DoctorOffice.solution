@{
  Layout = "_Layout";
}

@model Office.Models.Doctor

<h2>Doctor Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>

@* @Html.HiddenFor(model => model.SpecialtyId)
<h3>@Html.DisplayNameFor(model => model.Specialty): @Html.DisplayFor(model => model.Specialty)</h3> *@

@if(@Model.JoinEntities.Count == 0)
{
  <p>This Doctor Does Not Contain Any Patient</p>
}
else
{
  <h4>Patient Doctors</h4>
  <ul>
  @foreach(var join in Model.JoinEntities)
  {
    <li>@join.Patient.Name</li>
  }
  </ul>
}

<p>@Html.ActionLink("Add Patient", "Create", "Patients")</p>

<p>@Html.ActionLink("Doctor List", "Index")</p>


<p>@Html.ActionLink("Delete Doctor", "Delete", new { id = Model.DoctorId })</p>

<p>@Html.ActionLink("Edit Doctor", "Edit", new { id = Model.DoctorId })</p>
 